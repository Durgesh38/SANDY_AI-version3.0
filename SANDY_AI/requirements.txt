# ── Web Server ──────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
websockets>=12.0

# ── Report Generation ───────────────────────────────────────────────
reportlab>=4.0.0

# ── Voice (optional, for CLI voice_input.py only) ────────────────────
# SpeechRecognition>=3.10.0   # Browser handles voice via Web Speech API
# pyaudio>=0.2.13
# pyttsx3>=2.90

# ── Security tools — install via apt ────────────────────────────────
# apt install nmap nikto sqlmap hydra gobuster -y
